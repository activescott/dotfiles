[commit]
	gpgsign = true

[gpg]
	format = ssh

[gpg "ssh"]
  allowedSignersFile = ~/.ssh/authorized_keys

	# local ssh key: No need to set program!

[user]
	# local key:
	signingkey = ~/.ssh/id_rsa.pub
