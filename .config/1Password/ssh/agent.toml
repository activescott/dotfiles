# https://developer.1password.com/docs/ssh/agent/config

# Control the order keys are used by the agent here

[[ssh-keys]]
item = "SSH Key OP2024A"
vault = "Personal"

[[ssh-keys]]
item = "RapidSOS RSA SSH Key"
vault = "Personal"
